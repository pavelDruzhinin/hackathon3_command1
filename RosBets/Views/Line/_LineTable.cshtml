@model  IEnumerable<RosBets.Models.Match>
    
@Html.Hidden("MatchEventsNotifierEntity", (object)ViewBag.MatchEventsNotifierEntity)

@foreach (var match in Model)
{
    @Html.Partial("_MatchInfo", match)
}
